/*
1.指针就是地址，地址就是指针---指针所存的是变量的地址 
2。指针就是存放内存单元编号的变量，或者说指针变量就是存放地址的变量
指针的本质就是一个操作受限的非负整数（指针不允许相加，比如房子的两个门牌号相加没有意义，也不能乘除，但是指针可以相减） 


深入理解指针，必须要讲透以下5个点：
1.指针是一个普通的变量
2.既然指针是变量，那么肯定有自己的类型
3.既然指针是变量，那么肯定有自己的值
4.只不过指针的值跟一般变量的值不太一样，指针的值是一个"地址"
5.指针指向的数据

你家的门牌号就是指针，指向你家，但不是你家 
 
*/

#include <stdio.h>

int main(void)
{


printf("\n");

system("pause");
return 0;
}


